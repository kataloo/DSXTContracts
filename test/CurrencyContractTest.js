const CurrencyContract = artifacts.require('CurrencyContract');

contract('CurrencyContract', accounts => {
    describe('it should compile', () => {
        it('should allow owner to call replenish function', async function() {
            console.log('running');
        });
    });
});